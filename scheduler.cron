# Symlink this to /etc/cron.d
# Start the app
* * * * * pi ! ps aux | grep python | grep main.py | grep -v grep && sleep 15 && /usr/bin/python3 /home/pi/Escape-Room-Clock/main.py 
# Get automatic updates
*/10 * * * * root /bin/bash /home/pi/Escape-Room-Clock/updater.sh
